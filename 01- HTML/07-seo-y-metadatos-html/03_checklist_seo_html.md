# Checklist de SEO HTML

- La pagina tiene un `title` claro.
- La pagina tiene `meta description`.
- Existe un solo `h1`.
- La estructura del contenido es entendible.
- Las imagenes importantes tienen `alt`.
