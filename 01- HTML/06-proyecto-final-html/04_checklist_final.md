# Checklist final de HTML

- La pagina tiene estructura completa.
- Existe solo un `h1` por pagina.
- Hay `header`, `main` y `footer` cuando corresponde.
- Las imagenes tienen `alt` util.
- Los formularios tienen `label`.
- La jerarquia del contenido se entiende rapido.
